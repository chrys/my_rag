google-genai
litellm
python-dotenv
sentry-sdk
markdown
pypdf
numpy

# Optional vector-indexing dependencies for local/postgres RAG features live in
# requirements-ai.txt so the default install stays small enough for production.


# Django stack
django
django-unfold
djangorestframework==3.14.0
django-cors-headers==4.3.0
django-extensions==3.2.3
whitenoise==6.6.0

# Testing
pytest==7.4.3
pytest-django==4.7.0
pytest-mock==3.12.0

# WSGI & Server
gunicorn==21.2.0

# Database
 psycopg[binary]

